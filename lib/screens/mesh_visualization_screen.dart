import 'package:flutter/material.dart';

class MeshVisualizationScreen extends StatelessWidget {
  const MeshVisualizationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Mesh Monitor")),
      body: const Center(
        child: Text("Node monitoring coming soon"),
      ),
    );
  }
}